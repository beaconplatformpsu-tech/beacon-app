/**
 * Support messages are user-generated operational data.
 * They are intentionally not seeded by the public platform seed.
 */
export const getSupportMessages = (_timestamp: string): Record<string, never> => {
  return {};
};
